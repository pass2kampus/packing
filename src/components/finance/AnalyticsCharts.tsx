
import { Card, CardHeader, CardTitle, CardContent } from "@/components/ui/card";

export function AnalyticsCharts() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Analytics & Trends</CardTitle>
      </CardHeader>
      <CardContent>
        <div className="text-gray-600">Visualize spending with sleek charts and reports (WIP).</div>
      </CardContent>
    </Card>
  );
}
