
import { Card, CardHeader, CardTitle, CardContent } from "@/components/ui/card";

export function PartTimeJobLog() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Part-Time Job Logging</CardTitle>
      </CardHeader>
      <CardContent>
        <div className="text-gray-600">Enter hours and income from student jobs.</div>
      </CardContent>
    </Card>
  );
}
