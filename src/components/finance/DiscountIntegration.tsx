
import { Card, CardHeader, CardTitle, CardContent } from "@/components/ui/card";

export function DiscountIntegration() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Student Discount Integration</CardTitle>
      </CardHeader>
      <CardContent>
        <div className="text-gray-600">Maximize savings using exclusive student discounts.</div>
      </CardContent>
    </Card>
  );
}
